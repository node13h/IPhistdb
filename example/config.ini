[App]
# Timezone of the dates in the dhcpd logfiles
timezone=Europe/Vilnius

[MySQL]
host=localhost
user=iphistdb
pass=
db=iphistdb

[Maintenance]
# This should be higher than number of logrotated dhcpd logs
# or else files will be reprocessed several times.
# Program knows how to deal with duplicate records, so it is
# not a big deal anyway.
forget_files_after=31
